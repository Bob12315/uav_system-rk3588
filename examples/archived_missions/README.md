# Archived Action Mission references

These templates are historical behavior references. They are not loaded by the Web catalog,
production deployment profiles, or the formal mission validator. Current supported templates are
`drop_two_targets_v2`, `recon_gps_v2`, and `rescue_2026_full_auto_v2` under `config/action_missions/`.
