// app_state.js — Shared container state for UAV Action Console
// Extracted from app.js (WU-2B-min).  Only low-risk container variables
// that are mutated in place (not reassigned) are extracted here.
window.UavAppState = {
  history: [],
};
